.DS_Store
.env
.venv/
__pycache__/

# The git law (design doc v2.2 Section 16.3): projects are DELIVERABLES, not
# factory source. Nothing any department produces for a project is ever
# committed to the factory's repository — no intent files, no packets, no code,
# no ledgers. Each project graduates to its own repo when Deploy exists.
projects/
