# Packet: P-007 — Family Two: OpenAI Adapter
# One job: name the environment variables the Switchboard expects. Placeholders
# only — a real value must never be committed to this file.
# Version: 0.7.0
#
# Copy to .env at this project root and fill in. .env is gitignored.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
